# 🐛 نسخه DEBUG - برای پیدا کردن مشکل

## ⚠️ این نسخه برای دیباگ است!

این نسخه **console logging** زیادی داره که به ما کمک می‌کنه ببینیم دقیقاً چه اتفاقی میفته.

## 📦 فایل‌های DEBUG:

- `AdminApp-Debug.exe` (2.2 MB)
- `AdminApp-Debug.msi` (3.2 MB)

## 🔍 نحوه استفاده:

### مرحله 1: نصب
- نسخه قدیمی رو uninstall کنید
- یکی از فایل‌های بالا رو نصب کنید

### مرحله 2: باز کردن Developer Console
این خیلی مهمه! بعد از باز شدن برنامه:

**در Windows:**
1. برنامه رو باز کنید
2. کلید `F12` رو بزنید
3. یا `Ctrl + Shift + I`
4. یک پنجره Developer Tools باز میشه

### مرحله 3: Login کنید
```
Email: admin@demo.com
Password: admin123
```

### مرحله 4: Screenshot بگیرید
وقتی Network Error داد:
1. از Console tab یک screenshot بگیرید
2. تمام پیام‌ها رو به من بفرستید

## 🔍 چی باید توی Console ببینیم:

اگر همه چیز درست باشه، باید این پیام‌ها رو ببینید:

```
🔍 Environment Check:
- isTauri: true
- window.__TAURI__: true
- API_URL: http://108.143.173.222:3000/api

🚀 tauriHttpRequest called with config: {...}
📡 Making Tauri HTTP request to: http://108.143.173.222:3000/api/auth/login
📤 Request options: {...}
✅ Response received: {...}
```

اگر مشکل باشه، ممکنه ببینید:

```
❌ Not in Tauri environment!
```

یا

```
❌ Tauri HTTP Error: ...
```

## 📊 اطلاعات مهم برای ما:

لطفاً این اطلاعات رو بفرستید:

1. **Screenshot از Console** (بعد از F12)
2. **متن کامل Error Message**
3. **آیا `isTauri` برابر `true` هست یا `false`؟**
4. **آیا پیام `🚀 tauriHttpRequest called` رو می‌بینید؟**

با این اطلاعات می‌تونیم دقیقاً بفهمیم مشکل کجاست!

---

**Build**: 19601607421
**Date**: November 22, 2025
**Purpose**: Debug logging برای پیدا کردن علت Network Error
